# iPhoneだけで更新する手順

この版は `index.html` だけで動きます。
背景PNGや `assets` フォルダのアップロードは不要です。

1. ZIPをiPhoneの「ファイル」アプリで展開
2. GitHubのリポジトリをSafariで開く
3. 既存の `index.html` を削除または上書き
4. このフォルダ内の `index.html` をアップロード
5. `Commit changes` を押す
6. 公開URLの末尾に `?v=0.6.3` を付けて開く

表示例:
https://ユーザー名.github.io/リポジトリ名/?v=0.6.3
