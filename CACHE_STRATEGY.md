# GitHub Pages キャッシュ対策

このプロジェクトは `index.html` 単一ファイル構成（外部の `.js` / `.css` を読み込んでいない）ため、
一般的な「`<script src="app.js?v=1.2.3">` にクエリを付けてキャッシュを回避する」手法がそのままでは使えません。
以下の2段構えで対応します。

## 今すぐできる対策（index.html側・実施済み）

`<head>` に以下を追加し、ブラウザに対して「このHTMLはキャッシュせず、毎回サーバーに再検証しに行ってほしい」と伝えています。

```html
<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
<meta http-equiv="Pragma" content="no-cache">
<meta http-equiv="Expires" content="0">
```

**注意**：`<meta http-equiv>` によるキャッシュ制御は、実際のHTTPレスポンスヘッダーによる指定より弱く、
すべてのブラウザ・すべての状況（特にモバイルのbfcache等）で確実に効くとは限りません。
GitHub Pages は静的ホスティングのため、レスポンスヘッダー自体をこちらで自由に設定することができない
（Jekyllのプラグインやカスタムサーバーを使わない前提）ので、これは「保険」程度の位置づけです。

## 確実な対策（運用でカバーする）

GitHub Pages 自体は、`git push` してから数分程度でCDN（Fastly）側のキャッシュも更新されるため、
実運用上いちばん効果的なのは **プレイリンクにバージョンクエリを付けて共有する** ことです。

```
https://<YOUR_USERNAME>.github.io/<REPO_NAME>/?v=0.6.1
```

`index.html` 自体はクエリパラメータを一切参照しませんが、URLとしては `?v=0.6.0` の版と
`?v=0.6.1` の版は別物としてブラウザ・CDNの双方に扱われるため、これだけで新しいバージョンが
確実に取得されます。README のプレイリンクや、SNS等で共有するリンクは、
**リリースのたびに `?v=` の値をそのバージョン番号に更新する**運用にしてください。

新しいバージョンを出すたびのチェックリスト：

1. `index.html` 内のバージョン表記（`<title>` / ハブ画面 / ゲーム画面ヘッダー）を更新する
2. 更新履歴パネルに変更点を追記する
3. README・共有リンクの `?v=` を新しいバージョン番号に更新する
4. 下記「初回コミット・タグ付け手順」と同じ要領でコミット・タグを打つ

## 将来、JS/CSSを外部ファイルに分割した場合

その際は元々ご存知の手法がそのまま使えます。

```html
<link rel="stylesheet" href="style.css?v=0.6.1">
<script src="app.js?v=0.6.1"></script>
```

バージョン番号は `index.html` 内の表示バージョンと必ず一致させてください
（不一致に気づきにくいバグの温床になるため、可能であればビルドスクリプトで自動置換する運用を推奨します）。
