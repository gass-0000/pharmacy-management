# はじめてのGitHub公開ガイド（パソコン操作だけでOK・コマンド不要）

「ターミナルを使わずに、ブラウザの操作だけで薬局ゲームをネット上に公開する」手順です。
上から順番に進めれば大丈夫です。ひとつずつ確認しながら進めてください。

---

## 全体の流れ（まずはここだけ把握すればOK）

1. GitHubのアカウントを作る（無料）
2. 「リポジトリ」という置き場所を1つ作る
3. そこに5つのファイルをドラッグ＆ドロップでアップロードする
4. 「GitHub Pages」という機能をONにする
5. 発行されたURLをブラウザで開くと、ゲームが遊べる

コマンド入力は一切ありません。

---

## ステップ1：GitHubのアカウントを作る

すでにアカウントをお持ちの場合はステップ2へ進んでください。

1. ブラウザで `https://github.com/` を開く
2. 右上の **Sign up**（サインアップ）をクリック
3. メールアドレス・パスワード・ユーザー名を入力して案内に従って進める
4. メール認証（届いたメールのリンクをクリック）をすませる

ここで決めた「ユーザー名」は、あとで公開URLの一部になります（例：`example-user`）。

---

## ステップ2：リポジトリ（置き場所）を作る

1. GitHubにログインした状態で、右上の **＋** ボタンをクリック
2. 出てきたメニューから **New repository** をクリック
3. 以下を入力する
   - **Repository name**：好きな名前（例：`pharmacy-game`）半角英数字とハイフンが無難です
   - **Public / Private**：**Public** を選ぶ（GitHub Pagesで無料公開するにはPublicが必要）
   - **Add a README file** などのチェックボックスは **すべてOFF（チェックなし）** のままでOK
4. 一番下の緑色の **Create repository** ボタンをクリック

これで空っぽのリポジトリが1つできました。

---

## ステップ3：ファイルをアップロードする

1. 作成されたリポジトリのページを開く（ステップ2の直後ならそのまま表示されています）
2. 画面中央あたりの **Add file** というボタン（またはリンク）をクリックし、**Upload files** を選ぶ
3. パソコンの中にある、お渡しした5つのファイルをまとめてドラッグ＆ドロップする
   - `index.html`
   - `.gitignore`
   - `SETUP.md`
   - `CACHE_STRATEGY.md`
   - `GIT_SETUP.md`

   > **`.gitignore` がドラッグ&ドロップで反応しない場合**：
   > ファイル名が「.」から始まるファイルは、パソコンによっては見えなかったり選択しづらいことがあります。
   > その場合は `.gitignore` を後回しにして、残り4ファイルだけ先にアップロードしてしまって問題ありません
   > （なくてもゲームは正常に動きます。あとで同じ「Add file → Upload files」から追加もできます）。

4. 画面下までスクロールし、**Commit changes** の緑ボタンをクリック
   - コメント欄（Commit message）は空でも「Add files via upload」のままでもOKです

これでファイルがアップロードされました。リポジトリのファイル一覧に `index.html` などが表示されていれば成功です。

---

## ステップ4：GitHub Pagesを有効にする

ここが「ネット上に公開する」ための設定です。

1. リポジトリページ上部のタブから **Settings**（歯車マークの近く）をクリック
2. 左側メニューの一覧から **Pages** をクリック
3. **Build and deployment** という項目の **Source** が `Deploy from a branch` になっていることを確認
4. その下の **Branch** の設定を、`main` ＋ `/ (root)` に変更する
   - プルダウンから `main` を選ぶ → 隣のプルダウンは `/ (root)` のまま
5. **Save** ボタンをクリック

保存すると、ページの上のほうに緑色の帯で

> Your site is live at `https://<あなたのユーザー名>.github.io/<リポジトリ名>/`

のようなメッセージが出ます（出るまで数十秒〜数分かかることがあります。出ない場合はページを再読み込みしてみてください）。

---

## ステップ5：公開されたゲームを開く

1. ステップ4で表示されたURLをコピーする
2. ブラウザの新しいタブに貼り付けて開く
3. 薬局マネジメントゲームの画面が表示されれば完成です

もし真っ白な画面や404エラーが出る場合：

- Pagesの設定直後は反映まで数分かかることがあるので、少し待って再読み込みしてみてください
- URLの末尾に `/` が付いているか確認してください（例：`.../pharmacy-game/`）
- Settings → Pages のページに戻り、公開URLが表示されているか再確認してください

---

## 今後、ファイル（index.htmlなど）を修正したときの更新方法

コマンドを使わなくても、Web上だけで更新できます。

1. リポジトリのファイル一覧から、更新したいファイル（例：`index.html`）をクリックして開く
2. 右上の鉛筆マーク（**Edit this file**）をクリック
   - もしくは、いったんそのファイルを画面上で選択 → 削除 → 新しい版を「Add file → Upload files」で
     再アップロードする方法でもOKです（大きなファイルはこちらの方が簡単です）
3. 編集または再アップロード後、画面下の **Commit changes** をクリックして保存する
4. 数分待つと、公開URLの内容も自動的に新しい版に更新されます

> 更新してもブラウザに古い画面が残る場合は、`CACHE_STRATEGY.md` に書いた通り、
> 共有するリンクの末尾に `?v=0.6.2` のようにバージョン番号を付けて開き直してみてください。

---

## 用語のかんたんな説明

| 用語 | かんたんに言うと |
|---|---|
| GitHub | プログラムのファイルを保存・公開できる、ネット上の置き場所サービス |
| リポジトリ (repository) | 1つのプロジェクト専用の「フォルダ」のようなもの |
| コミット (commit) | 「この状態で保存します」という操作。上書き保存に近いイメージ |
| GitHub Pages | リポジトリの中身を、そのままWebサイトとして公開できる無料機能 |
| main（ブランチ） | 基本的には「本番用の1本の履歴」くらいの理解でまずは十分です |

---

## もし余裕があれば：次回からもっと楽にする方法（任意）

毎回ブラウザでアップロードし直すのが面倒に感じたら、**GitHub Desktop**という無料アプリを使うと、
パソコンのフォルダを編集するだけで「変更を保存してGitHubに送る」がボタン1つでできるようになります。
（`https://desktop.github.com/` からインストール）
これはコマンドの知識がなくても使えるので、慣れてきたら検討してみてください。
最初はステップ1〜5のWeb操作だけで十分です。
