# 調剤薬局ひまわり - 薬局マネジメントゲーム

> **このファイルについて**：このリポジトリには既存の `README.md` があるため、
> 上書きせず本ファイル（`SETUP.md`）として追加しています。内容は「プロジェクト概要・遊び方・
> バージョン管理の型・現状の制約」です。必要であれば、この内容を既存の `README.md` に
> あとから手動でマージしていただいても構いません。

Mother2風のドット絵UIで遊ぶ、ブラウザ完結型の薬局経営シミュレーションゲームです。
外部依存なしの単一HTMLファイル（`index.html`）として動作し、ビルド不要でそのまま開けます。

## 概要

- 4〜6月（12週間・3か月）の薬局経営をシミュレーションします。
- 毎週「人員配置」「発注方針」「重点方針」などを判断し、その結果が翌週以降に影響します。
- 経営理念・予算案の選び方によって、本部・社員・患者からの評価（5指標評価）が変わります。
- スタッフには疲労・経験値・レベルの概念があり、無理な配置は効果が下がったり休職・退職につながることがあります。
- セーブ機能はまだ実装しておらず、1プレイは3か月分をひと続きで遊びきる想定です（詳細は下記「現状の制約」参照）。

## 遊び方

### オンラインで遊ぶ（GitHub Pages）

このリポジトリを GitHub Pages で公開している場合、以下の URL からブラウザで直接遊べます。

```
https://<YOUR_USERNAME>.github.io/<REPO_NAME>/?v=0.6.1
```

`<YOUR_USERNAME>` と `<REPO_NAME>` は、実際に登録した GitHub のユーザー名・リポジトリ名に置き換えてください。
（例：`https://example-user.github.io/pharmacy-management-game/?v=0.6.1`）

末尾の `?v=0.6.1` はキャッシュ対策のためのバージョンクエリです。新しいバージョンをリリースしたら、
共有するリンクの数字もそのバージョンに更新してください（詳細は `CACHE_STRATEGY.md` を参照）。

スマートフォン・PC どちらのブラウザでも動作します（追加インストール不要）。

### ローカルで遊ぶ

1. このリポジトリを `git clone` するか、`index.html` をダウンロードします。
2. `index.html` をブラウザ（Chrome / Safari / Edge 等）で直接開きます。
3. サーバーを立てる必要はありません（`file://` で開いても動作します）。

## 開発について

- 単一ファイル構成（HTML / CSS / JavaScript がすべて `index.html` に内包されています）。
- 外部ライブラリ・CDN依存はありません。
- 継続開発はチャットベース（ChatGPT / Claude 等）で行われており、`index.html` 内のコメントに
  各修正の意図・設計判断が残されています。大きな変更を加える際は既存のコメントも参考にしてください。

## バージョン履歴の書き方（型）

`index.html` 内のハブ画面「更新履歴」パネルに、以下の型でバージョンごとの変更点を追記していきます。
新しいバージョンほど上（新しい順）に並べます。

```html
<div class="event"><b>vX.Y.Z</b> ここに変更点を1〜3文程度で簡潔に記載する。
「何を」「なぜ」「プレイヤーにどう影響するか」が分かる粒度が望ましい。</div>
```

このリポジトリの README でも、`CHANGELOG` としてリリースごとに同じ内容を残すことを推奨します
（`CHANGELOG.md` を別途作成する場合は、下記のような形式にしてください）。

```markdown
## vX.Y.Z - YYYY-MM-DD
- 変更点1
- 変更点2
- 修正したバグ（あれば）
```

### バージョン番号の付け方の目安

- `vX.0.0`：大きな仕様追加・フェーズ区切り（例：v0.6.0 = 育成システム導入）
- `vX.Y.0`：機能追加・システム拡張（例：v0.5.2 = 欠員対応の独立イベント化）
- `vX.Y.Z`：バグ修正・表現調整・品質改善のみ（例：v0.6.1 = 表示バグ修正）

次回以降は `v0.6.2`（バグ修正・微調整）、`v0.7.0`（新機能追加）のように、
変更の性質に応じてどの桁を上げるかを判断してください。

## 現状の制約

- **セーブ機能は未実装です。** ブラウザをリロードするとプレイ状況は失われます。1プレイは3か月分を通しで遊ぶ想定です。
- ローカルストレージ・サーバー保存のいずれも未対応です（将来的な実装候補）。
- スマートフォンのブラウザでの動作を主眼にレイアウトしていますが、レスポンシブ対応は簡易的なものです。
- 因果関係エンジン（`decisionLog` / `causeEdges` 等）はデータ構造として存在しますが、
  プレイ画面への本格接続は一部の画面（配置プレビュー・「なぜそうなった？」欄）に限られています。
- ゲームバランス（利益しきい値・疲労閾値等）は暫定値が多く、実プレイテストによる継続調整を前提としています。

## ライセンス

（未設定。公開範囲・再利用条件を決めたら、ここに追記してください。）
